
import React from 'react'

const Button = ({name}) => {
  return (
    <button className='rounded-lg px-3 bg-slate-600 text-xs py-1 text-white' onClick={()=>{}}>{name}</button>
  )
}

export default Button